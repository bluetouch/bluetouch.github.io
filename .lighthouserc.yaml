ci:
  collect:
    staticDistDir: ./dist/apps/bluetouch-web
  assert:
    preset: lighthouse:no-pwa
    assertions:
      max-potential-fid:
        - "error"
        - minScore: 0.2
  upload:
    # upload options here
  server:
    # server options here
  wizard:
    # wizard options here
